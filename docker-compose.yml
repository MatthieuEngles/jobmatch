version: '3.8'

services:
  gui:
    build:
      context: .
      dockerfile: app/gui/Dockerfile
    ports:
      - "${GUI_PORT:-8085}:8080"
    environment:
      - CV_INGESTION_URL=${CV_INGESTION_URL}
      - MATCHING_URL=${MATCHING_URL}
    depends_on:
      - cv-ingestion
      - db
    networks:
      - jobmatch-network

  cv-ingestion:
    build:
      context: .
      dockerfile: app/cv-ingestion/Dockerfile
    ports:
      - "${CV_INGESTION_PORT:-8081}:8081"
    env_file:
      - app/cv-ingestion/.env
    environment:
      - DATABASE_URL=${DATABASE_URL}
    networks:
      - jobmatch-network

  offre-ingestion:
    build: ./app/offre-ingestion
    ports:
      - "${OFFRE_INGESTION_PORT:-8082}:8082"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - FRANCE_TRAVAIL_API_KEY=${FRANCE_TRAVAIL_API_KEY}
    networks:
      - jobmatch-network

  matching:
    build: ./app/matching
    ports:
      - "${MATCHING_PORT:-8083}:8083"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - CV_INGESTION_URL=${CV_INGESTION_URL}
      - OFFRE_INGESTION_URL=${OFFRE_INGESTION_URL}
    depends_on:
      - cv-ingestion
      - offre-ingestion
    networks:
      - jobmatch-network

  db:
    image: postgres:16-alpine
    ports:
      - "${DB_PORT:-5433}:5432"
    environment:
      - POSTGRES_USER=${POSTGRES_USER:-jobmatch}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-jobmatch}
      - POSTGRES_DB=${POSTGRES_DB:-jobmatch}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - jobmatch-network

  redis:
    image: redis:7-alpine
    ports:
      - "${REDIS_PORT:-6379}:6379"
    networks:
      - jobmatch-network

networks:
  jobmatch-network:
    driver: bridge

volumes:
  postgres_data:
